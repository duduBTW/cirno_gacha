import styled from "@emotion/styled";

export const EmoteGridWrapper = styled.div`
  display: grid;
  gap: 30px;

  grid-template-columns: repeat(auto-fill, minmax(162px, 1fr));

  margin: 60px 180px;
`;
